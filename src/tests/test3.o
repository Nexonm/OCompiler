// Class with Methods and Local Variables
class Calculator is
    var result : Integer(0)

    method add(a : Integer, b : Integer) : Integer is
        var sum : Integer(0)
        sum := a.Plus(b)
        return sum
    end

    method getValue() : Integer is
        return result
    end

    this() is
    end
end
