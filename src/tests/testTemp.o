// Class with member variables
class A is
    var count : Integer(222)

    method getCount() : Integer is
        return count
    end

    this() is
    end
end

class Counter extends A is
    var isActive : Boolean(true)

    //method inc() is
    //    count := count.Plus(Integer(1))
    //end

    //method add(a : Integer) is
    //    count := count.Plus(a)
    //end

    this() is
    end
end
