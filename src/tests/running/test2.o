// Name: Running test for overriding, should be run to check if it's working
// Description: Override parent class
// Expected result: pass
// Expected fail stage: none

class Value is
    var value : Integer(10)

    method getValue() : Integer is
        return value
    end

    method setValue(v : Integer) is
        value := v
    end

    this() is
    end
end

class DoubleValue extends Value is

    method getValue() : Integer is
        return Integer(10)
    end

    this() is
    end
end

class Start is
    this() is
    end

    method start() is
        var dv : DoubleValue()
        dv.setValue(Integer(5))
        var p : Printer()
        p.print(dv.getValue())
    end
end